---
import Layout from '~/layouts/PageLayout.astro';
import ResetPasswordForm from '~/components/widgets/ResetPasswordForm';

const metadata = {
  title: 'Nueva Contrasena - SG Cloud',
  robots: { index: false, follow: false },
};
---
<Layout metadata={metadata}>
  <section class="px-4 py-16 sm:px-6 lg:px-8 lg:py-20 mx-auto max-w-5xl">
    <ResetPasswordForm client:load locale="es" />
  </section>
</Layout>
