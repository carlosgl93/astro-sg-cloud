---
import Layout from '~/layouts/PageLayout.astro';
import ForgotPasswordForm from '~/components/widgets/ForgotPasswordForm';

const metadata = {
  title: 'Reset Password - SG Cloud',
  robots: { index: false, follow: false },
};
---
<Layout metadata={metadata}>
  <section class="px-4 py-16 sm:px-6 lg:px-8 lg:py-20 mx-auto max-w-5xl">
    <ForgotPasswordForm client:load locale="en" />
  </section>
</Layout>
